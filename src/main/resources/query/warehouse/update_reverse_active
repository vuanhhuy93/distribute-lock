query {
   user_db_v2_updatereversenumberonwarehouse(args:{productid: $product_id, updatenumber: $change_number} ){
    product_id
    total
    active_number
    reverse_number
    version
  }
}
         